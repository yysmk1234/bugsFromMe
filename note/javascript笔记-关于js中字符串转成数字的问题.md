## 关于js 中字符串转数字出现的问题

   最近的项目中出现了前端给后端传数据的时候，发现传过来的参数无法正常的表示原来的数值。
   具体的情况为请求的参数为类似 '"1"' 这种表现形式，于是在前端进行判断和转型。
   但是，如果直接把 "1" 转成Number 类型的数据会返回NaN，因为js默认如果不是数字开头的话，就返回NaN，所以要在转型之前先把字符串处理一下，把原数据的
   双引号替换一下  .replace(/\"/g,""),处理完之后再转换成数字就不会出现NaN 的情况
